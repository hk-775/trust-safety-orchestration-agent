{
  "Comment": "SafetyAgent Bulk Action Workflow — processes coordinated attack enforcement",
  "StartAt": "ValidateConfidence",
  "States": {
    "ValidateConfidence": {
      "Type": "Choice",
      "Choices": [
        {
          "And": [
            {
              "Variable": "$.confidence_score",
              "NumericGreaterThanEquals": 0.85
            },
            {
              "Variable": "$.user_ids[0]",
              "IsPresent": true
            }
          ],
          "Next": "ExecuteBulkAction"
        }
      ],
      "Default": "EscalateToHumanReview"
    },

    "ExecuteBulkAction": {
      "Type": "Task",
      "Resource": "${BulkEnforcementArn}",
      "Parameters": {
        "case_id.$": "$.case_id",
        "user_ids.$": "$.user_ids",
        "action.$": "$.action",
        "violation_type.$": "$.violation_type",
        "attack_pattern.$": "$.attack_pattern",
        "is_bulk": true
      },
      "ResultPath": "$.enforcement_result",
      "Retry": [
        {
          "ErrorEquals": ["States.TaskFailed"],
          "IntervalSeconds": 5,
          "MaxAttempts": 2,
          "BackoffRate": 2.0
        }
      ],
      "Catch": [
        {
          "ErrorEquals": ["States.ALL"],
          "ResultPath": "$.error",
          "Next": "EscalateToHumanReview"
        }
      ],
      "Next": "BulkActionComplete"
    },

    "EscalateToHumanReview": {
      "Type": "Task",
      "Resource": "${EscalationHandlerArn}",
      "Parameters": {
        "case_id.$": "$.case_id",
        "user_id.$": "$.user_id",
        "confidence": {
          "scores": {
            "bot_farm.$": "$.confidence_score"
          },
          "primary_violation": "bot_farm",
          "primary_score.$": "$.confidence_score"
        },
        "reason": "bulk_action_low_confidence"
      },
      "ResultPath": "$.escalation_result",
      "Retry": [
        {
          "ErrorEquals": ["States.TaskFailed"],
          "IntervalSeconds": 2,
          "MaxAttempts": 3,
          "BackoffRate": 2.0
        }
      ],
      "Next": "BulkActionComplete"
    },

    "BulkActionComplete": {
      "Type": "Succeed"
    }
  }
}
